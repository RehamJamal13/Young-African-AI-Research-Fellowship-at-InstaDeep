hydra-core==1.3.2  # Or latest stable version of Hydra
jax==0.4.13        # For JAX (ensure compatibility with your hardware)
flax==0.6.1        # For Flax (Neural Network Library for JAX)
optax==0.1.4       # Optimizer library compatible with JAX
tqdm==4.66.1       # For progress bars
matplotlib==3.8.0  # For plotting graphs
omegaconf==2.3.0   # For Hydra configuration (comes with Hydra)
